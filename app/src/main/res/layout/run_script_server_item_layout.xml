<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <CheckBox
        android:id="@+id/cbServerChosen"
        android:layout_width="30dp"
        android:layout_height="30dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />
    <TextView
        android:id="@+id/tvServerName"
        tools:text="Server Name"
        android:textSize="24sp"
        app:layout_constraintStart_toEndOf="@id/cbServerChosen"
        app:layout_constraintTop_toTopOf="@id/cbServerChosen"
        app:layout_constraintBottom_toBottomOf="@id/cbServerChosen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="30dp"
        />
    <Button
        android:id="@+id/btnShowOutput"
        android:text="Show output"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cbServerChosen"
        android:visibility="gone"
        />
    <Button
        android:id="@+id/btnShowScriptRunning"
        android:text="Running ..."
        android:backgroundTint="@color/grey"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cbServerChosen"
        android:visibility="gone"
        />
</androidx.constraintlayout.widget.ConstraintLayout>